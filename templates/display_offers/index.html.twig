{% extends 'base.html.twig' %}

{% block title %}Hello DisplayOffersController!
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
		.title {
			color: red;
			font-size: 30px;
			letter-spacing: 1px;
		}
		.italic_name {
			box-shadow: inset 0 0 0 0 #54b3d6;
			color: #54b3d6;
			transition: color 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
		}
		.italic_name:hover {

			box-shadow: inset 100% 0 0 0 #54b3d6;
			color: red;
		}
	</style>

	<div class="example-wrapper">
		<h1 style="text-align:center;">Hello, here is the
			{{ controller_name }}
			page !</h1>

		<ul style="list-style-type: none;">
			{% if offers %}
				{% for element in offers %}
					<li>
						<div style=" padding:3%; text-align:center; width: 400px; margin: 0px auto; background-color: white; box-shadow: 0px 5px 20px #999;">
							<h3 style="letter-spacing:5px;">
								Job offer :
							</h3>
							<i class="italic_name">{{element.name}}</i>

							<h3>
								Description :
							</h3>
							<i style="font-size:50%">{{element.description}}</i>

							<h3>
								Business :
							</h3>
							<i class="italic_name">
								{{element.business.name}}</i>

							<h3>
								Skills :
							</h3>
							{% for skill in element.skills %}
								<i class="italic_name">{{skill.name}}</i>
								<br>
							{% endfor %}

						</li>
					{% endfor %}
				{% else %}
					<li>
						There is no offers for the moment.
					</li>
				{% endif %}

			</ul>
		</div>
	{% endblock %}
